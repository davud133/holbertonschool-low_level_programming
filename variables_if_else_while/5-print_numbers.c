#include <stdio.h>
/**
 * main - Entry point
 *
 * Description 'A program that prints numbers'
 *
 * Return: always 0 (succes)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
