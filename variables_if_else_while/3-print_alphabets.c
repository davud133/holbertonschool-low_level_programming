#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'A program that prints "Hello, Betty!" to the console.'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for(i = 97; i < 122; i++)
	{
		putchar(i);
	}
	int i;

	for(i = 65; i < 90; i++)
	{
		putchar(i);
	}
	putchar('\n);
	return (1);
}
